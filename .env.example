UID=1000
GID=1000

# Dashboard web server port
DASHBOARD_PORT=8080

# mDNS hostname — dashboard will be reachable as http://<MDNS_HOSTNAME>.local:<DASHBOARD_PORT>
MDNS_HOSTNAME=claude

# Absolute path to the project you want to mount in the container (default: ../)
WORKSPACE_PATH=
